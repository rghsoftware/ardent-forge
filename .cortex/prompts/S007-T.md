You are builder-ui, the frontend specialist on this team.

FEATURE CONTEXT:
Read these files before starting work:
- Context/Features/023-Exercise-Completion-Signal/Spec.md -- requirements and testable assertions
- Context/Features/023-Exercise-Completion-Signal/Steps.md -- task definitions
- CLAUDE.md -- project conventions
- .claude/rules/react-typescript.md -- test conventions

YOUR TASK: S007-T
Add two missing behavioral contracts to `-log-workout-finish-banner.test.tsx` (P20-008):

TEST 1 -- CIRCUIT filter (relates to P20-001 fix):
The `allActivitiesDone` derivation now filters out CIRCUIT groups. Add a test that verifies:
- A workout with one STRAIGHT_SETS group and one CIRCUIT group shows the all-done banner
  once all straight-set activities are marked done (even though circuit activities are not
  in skippedActivityIds)
- The circuit group's activities should NOT need to be in skippedActivityIds for the banner
  to appear

TEST 2 -- programmed workout:
The banner is rendered inside `{!isProgrammedWorkout && (...)}`. Add a test that verifies:
- The all-done banner is absent when `isProgrammedWorkout=true`, even when all activities
  are in skippedActivityIds

IMPORTANT CONTEXT:
- Read the full test file `-log-workout-finish-banner.test.tsx` carefully -- you are
  ADDING tests, not rewriting existing ones. S005-T ran before you and may have modified
  the file; read its current state before editing.
- Read the route file `log.$workoutId.tsx` to understand how isProgrammedWorkout is derived
  and where the CIRCUIT filter was added.
- For TEST 1: set up workoutState.loggedGroups with both a STRAIGHT_SETS group and a
  CIRCUIT group. Mark only the straight-set activities in skippedActivityIds.
- For TEST 2: find how isProgrammedWorkout flows into the component (it likely comes from
  workoutLog or workoutState -- inspect what field controls it).

FILES YOU OWN (only modify this file):
- `src/routes/_authenticated/__tests__/-log-workout-finish-banner.test.tsx`

UPSTREAM CONTRACTS: S005-T must have completed before you run (same file ownership).

CONTRACTS YOU MUST PRODUCE:
Write a task_done event when complete:
  bun run .claude/hooks/event-log/event-log.ts append --source builder-ui-s007 --type task_done --task S007-T

On error:
  bun run .claude/hooks/event-log/event-log.ts append --source builder-ui-s007 --type error --note "<reason>"

After finishing, run:
  bun run test -- --reporter=verbose src/routes/_authenticated/__tests__/-log-workout-finish-banner.test.tsx

All tests in the file must pass before emitting task_done.

Mark task #12 as completed via TaskUpdate when done.
