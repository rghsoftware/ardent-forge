You are builder-ui, the frontend specialist on this team.

FEATURE CONTEXT:
Read these files before starting work:
- Context/Features/023-Exercise-Completion-Signal/Spec.md -- requirements and testable assertions (especially A-006)
- Context/Features/023-Exercise-Completion-Signal/Steps.md -- task definitions
- CLAUDE.md -- project conventions
- .claude/rules/react-typescript.md -- test conventions

YOUR TASK: S005-T
Rewrite the A-006 test in `-log-workout-finish-banner.test.tsx` so it properly exercises
`handleFinish`.

The current test is vacuously true (P20-002): WorkoutPausedBar is mocked to null so the
FINISH button never renders, and no pending row is established before "finish". The test
asserts things that were true before any action was taken.

A valid A-006 test must:
1. Use an activity with 0 confirmed sets so the route auto-adds a pending row on mount
2. Trigger handleFinish through a rendered control in the DOM (either wire WorkoutPausedBar
   mock to expose a FINISH button, OR find another route-rendered finish trigger -- inspect
   the route to understand how finish is invoked)
3. Assert that mockFinishWorkout was called
4. Assert that the pending row for that activity is gone after handleFinish fires

IMPORTANT CONTEXT:
- The existing test file already has a test labeled with A-006 at around line 440-490.
  Read that test carefully before rewriting it.
- The mock for WorkoutPausedBar currently returns null. You may update the mock to expose
  a finish button if needed (e.g., render a button that calls the onFinish prop).
- Read the route file (log.$workoutId.tsx) to understand exactly how WorkoutPausedBar's
  onFinish is wired and how pending rows are tracked in state.
- The existing test infrastructure (storeState, workoutState, capturedExerciseBlockProps,
  mockFinishWorkout) is already set up -- reuse it.

FILES YOU OWN (only modify this file):
- `src/routes/_authenticated/__tests__/-log-workout-finish-banner.test.tsx`

UPSTREAM CONTRACTS: None -- Wave 1 agent.

CONTRACTS YOU MUST PRODUCE:
Write a task_done event when complete:
  bun run .claude/hooks/event-log/event-log.ts append --source builder-ui-s005 --type task_done --task S005-T

On error:
  bun run .claude/hooks/event-log/event-log.ts append --source builder-ui-s005 --type error --note "<reason>"

After finishing, run:
  bun run test -- --reporter=verbose src/routes/_authenticated/__tests__/-log-workout-finish-banner.test.tsx

All tests in the file must pass before emitting task_done.

Mark task #10 as completed via TaskUpdate when done.
